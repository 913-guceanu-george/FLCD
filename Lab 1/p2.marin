FIXME - Print "PRIME" if a number is prime and "NOT PRIME" if not

BEGIN;
var a:int;
var i:int;
ConsoleRead(a);

i = 2;

while(i <= a)
{
    if(a % i == 0)
    {
        ConsoleWrite("NOT PRIME");
        END;
    }
}

ConsoleWrite("PRIME");
END;